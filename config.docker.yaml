server:
  port: 8080
  jwt_secret: "change-me-in-production"
  jwt_expire_hours: 24

db:
  host: "127.0.0.1"       # 外部 PostgreSQL 地址
  port: 5432
  user: postgres
  password: "change-me"
  dbname: fanapi
  sslmode: disable
  max_open_conns: 100
  max_idle_conns: 20
  conn_max_idle_sec: 300

redis:
  addr: "127.0.0.1:6379"  # 外部 Redis 地址
  password: ""
  db: 0

nats:
  url: "nats://127.0.0.1:4222"  # 外部 NATS 地址

smtp:
  host: smtp.example.com
  port: 465
  user: no-reply@example.com
  password: ""
  from: "FanAPI <no-reply@example.com>"
